import { ResourceBarType } from "../types/components";
/* ============================================== Container(s) ========================================= */
// Resource Bar Container

/* ============================================== Interfaces ========================================= */

interface ResourceBarsProps {
	Health?: ResourceBarType;
	Mana?: ResourceBarType;
	Stamina?: ResourceBarType;
}
